MAD TRACKS LEVEL EDITOR PROGRESS
================================

IMPORT
---------

- implemented .ldo import, good enough for now
	MAD TRACKS ENGINE ISSUE:
	Only one UV coordinate is assigned per vertex,
	instead of assigning a list of UV coordinates per polygon.
	So the vertex needs to be duplicated if it's shared by 2 polygons mapped to
	different regions of the texture.
	=> it doubles the vertex count for every model in the game
	=> it makes Blender's "Lightmap Pack" impossible to use, since it considers that UV-mapping
	is assigned per polygon and gives vertices multiple UV coordinates.
	Meaning I need to find another solution for .ldl file export!

- started implementing level .ini import
	MAD TRACKS FILE FORMAT ISSUE:
	Level .ini files do not seem to respect the file specification,
	as they contain duplicate sections and their order matter because of trackparts.
	=> it makes Python's configparser and other .ini readers impossible to use, since they
	consider that sections are unique and their order is irrelevant.
	Meaning I had to write a dedicated .ini reader!

- started implementing a trackpart editor

- thinking about implementing .ldl import early, but it's not a bad thing
	/!\ It might require having all vertices imported beforehand (all .ldos supported)
	In Blender, it makes sense to have the lightmap applied to vertices.
	So I would need to pick the lightmap texture color at a XY coordinate and apply this color to a vertex color channel.
	/!\ Using vertex color channels will give a wrong translation, since lightmaps offer hard changes of color in the middle of a face.
	So try to manually apply two textures to a face to have the lightmap on top of the diffuse texture.
	To do that, I might need to diverge from Revolt's way of doing and apply texture/materials to the Blender objects.
	So per object we have two textures and two UV mappings (easy, they have things for that)
	How to assign a UV map to a texture?
	How to display the goddamn thing on screen?
	CHALLENGE: is it possible to modify the imported instances to add the lightmap texture and mapping?
	>> might need an ID per object to modify them in the order of the .ldl file (which seems to be the order of the .ini file)
All of this makes me think it's more valuable and easier to just focus on the export part.

TODO

- keep trying to import levels without trackparts to search for bugs
>> stopped at UkMiniGolfRace.ini which looks interesting, FrBis_Affiches.ldo looks wrong in terms of .ldo

- keep populating trackparts dictionary and editor trackparts, offsets are known by selecting the endpoint vertex and looking at the object's LOCAL location

- remove Z-rotation restriction since I sometimes want to rotate on X and Y even for simple trackpart sequences

- when importing geometry with multiple atomics, parent all objects to a Empty? Re-Volt Add-on has a .w import option that does that.

- big optimization: appending trackparts is really slow when importing levels, because we always work our way from the first trackpart instead of the second to last one
- another optimization: since I loop through all objects in the scene to search for trackparts of the same sequence, import trackparts only first to not loop through the entire world mesh

- are inverted trackparts also rotated properly when imported on their own?

- point out in the installation part of the repo that the madtracks_dir variable needs to be set
   to be able to import/export files.

- (can't do it) fix rotation errors when importing ANT_Race.ini, the icebergs are flipped (Gimbal lock?)
   
   
EXPORT
---------

Have a bit of fun and work on my Japan mod project.
What I need to export with Blender:
- .ldo models (already done before)
- level .ini (already done, custom trackparts will be .ldo models, don't even need the editor)
- .ldl lightmaps (can be discarded at first, use the light objects for a quick shading)
- .gra AI paths
What can be done by hand:
- Blender objects formatting for export scripts
- .ini descriptors
- .dds textures
- .dam level info

